consumer_groups:
  - id: consumer-group-s3bucket-internal
    plugins:
      # limit-count:
      #   count: 20000
      #   time_window: 1
      #   key_type: var
      #   key: consumer_name

      response-rewrite:
        headers:
          set:
            X-Debug-Consumer-Resolved: "$consumer_name"
