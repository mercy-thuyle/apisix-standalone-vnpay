services:
  # -------------------------------------------------------------------
  #  TIER 2 — UI / MANAGEMENT (con người thao tác)
  #  Áp cho: cmc, hyperiq, s3-admin, các *-admin UI, grafana (/d)
  #  Traffic do người bấm → req/s thấp. Dùng limit-req (leaky bucket) cho mượt + limit-conn nhỏ. Body đã siết riêng ở route CMC (client-control 10MB)
  # -------------------------------------------------------------------
  - id: tier-2-ui-management
    plugins:
      limit-req:
        rate: 20                  # req/s ổn định / IP
        burst: 40                 # mở 1 trang nhiều asset → cho vọt
        key_type: var
        key: remote_addr
        rejected_code: 429
        rejected_msg: "Slow down — UI rate limit"
        allow_degradation: true
      limit-conn:
        conn: 20
        burst: 10
        default_conn_delay: 0.1
        key_type: var
        key: remote_addr
        rejected_code: 429
        rejected_msg: "Too many UI connections"