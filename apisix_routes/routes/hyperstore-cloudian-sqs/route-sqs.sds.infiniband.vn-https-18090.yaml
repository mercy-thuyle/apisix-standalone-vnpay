routes:
  # ── SQS — sqs.sds.infiniband.vn ─────────────────────────────────────
  # → upstream-sqs.sds.infiniband.vn
  # NGINX: HTTP only, port 80 (SQS backend không có TLS termination riêng)
  #
  # → plugin_config: pc-qos-auth (limit-count/api-breaker/soft-limit, SHARED với IAM + STS)
  #
  # ⚠ KHÔNG GỘP với IAM/STS dù cùng plugin_config — SQS dùng upstream RIÊNG
  #   (upstream-sqs, không share với upstream-iam) nên vẫn cần route/service
  #   riêng. Chỉ có 1 port (nginx gốc dòng 733-742: `listen 80;` — HTTP only,
  #   SQS backend không có TLS termination riêng) nên không có gì để gộp
  #   port ở route này (khác IAM/STS đã gộp 2 route port-variant).
  #
  # ⚠ HTTP KHÔNG mã hoá trên đường truyền — nếu SQS message chứa dữ liệu
  #   nhạy cảm, cân nhắc thêm TLS termination cho domain này trong tương lai
  #   (ngoài phạm vi rate-limit, cần trao đổi riêng với team network/backend).
  #   limit-count key=remote_addr không phụ thuộc header auth nào nên vẫn
  #   hoạt động bình thường trên HTTP.
  #
  # NGUYÊN TẮC PHÂN TÁCH:
  #   plugin_config_id → QoS TẬP TRUNG — sửa ở plugin_configs/pc-qos-auth.yaml
  #   plugins (route)   → CHỈ đặc thù riêng route này:
  #                         proxy-rewrite : forward IP thật lên backend SQS
  - id: "route-sqs.sds.infiniband.vn-https-18090"
    name: "hyperstore-cloudian-sqs"
    status: 1
    service_id: "service-upstream-sqs"
    # plugin_config_id trỏ đến QoS profile — chứa limit-count/api-breaker/blacklist.
    # Đổi QoS profile → chỉ sửa dòng này, không đụng plugin bên dưới.
    # Các profile có sẵn (xem apisix_routes/services/):
    #   sdk               → S3 SDK/machine traffic, phơi internet, volume cao (4000 req/s)
    #   auth              → IAM/STS/SQS, phơi internet (pre-GA), critical, có circuit breaker + blacklist
    #   internal-console  → CMC/HyperIQ/S3-Admin, INTERNAL ONLY, không whitelist/UA/referer,
    #                              chỉ limit-conn (chống nội bộ tự flood) + blacklist (phản ứng nhanh khi
    #                              1 client nội bộ lỗi/spam — nhanh hơn sửa network ACL)
    #   ops               → Script/batch nội bộ + legacy domain, siết chặt (10 req/s)
    #   ops-offpeak       → Ops off-peak ban đêm 2am-6am, nới rộng (200 req/s)
    plugin_config_id: "plugin-config-qos-auth"
    uri: "/*"
    host: "sqs.sds.infiniband.vn"
    methods: ["GET", "POST", "PUT", "DELETE", "PATCH", "HEAD", "OPTIONS"]
    vars:
      - ["scheme", "==", "http"]
    plugins:
          # ── Forward IP thật lên backend S3 SQS ─────────────────────────────
      proxy-rewrite:
        headers:
          set:
            X-Real-IP: "$remote_addr"
            X-Forwarded-For: "$remote_addr"
            X-Cluster-Client-Ip: "$remote_addr"
            X-Forwarded-Proto: "$scheme"

      # ── DEBUG TẠM — log request/response body ra file riêng, giống route CMC.
      #    Dùng file-logger (không phải serverless-post-function) để tránh mọi
      #    rủi ro duplicate-key như đã gặp ở route CMC. ─────────────────────
      file-logger:
        path: "/usr/local/apisix/logs/services/sqs-http-debug-body.log"
        include_req_body: true
        include_resp_body: true
        log_format:
          time: "$time_iso8601"
          method: "$request_method"
          uri: "$request_uri"
          status: "$status"
          upstream_status: "$upstream_status"
          upstream_addr: "$upstream_addr"
          request_id: "$request_id"

    timeout:
      connect: 30
      send: 600
      read: 300
