# ── Secrets & credentials ─────────────────────────────────────
.env
secrets/.netrc

# ── Private keys ──────────────────────────────────────────────
certs/*.key

# ── Patched APISIX internals (generated by 1-patch-template-lua.sh) ──
ngx_tpl.lua
ngx_tpl.lua.orig
init.lua
init.lua.orig

# ── gitsync runtime (managed by git-sync, not committed) ──────
gitsync/

# ── Injected route files (cert injected, not committed) ───────
gitsync/apisix_routes/*.bak

# ── Logs ──────────────────────────────────────────────────────
logs/

# ── macOS ─────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride